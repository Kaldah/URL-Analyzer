# URL-Analyzer
URL-Analyzer is a lightweight web service for detecting potentially malicious URLs. It exposes a simple REST API (POST /analyze) that takes a URL as input and returns a risk assessment, including malicious and harmless vote counts.
